diff --git a/src/app/api/sumit/business-brain-checkout/route.ts b/src/app/api/sumit/business-brain-checkout/route.ts
index 3ae50d4..4581954 100644
--- a/src/app/api/sumit/business-brain-checkout/route.ts
+++ b/src/app/api/sumit/business-brain-checkout/route.ts
@@ -44,6 +44,12 @@ export async function POST(req: Request) {
   const name = typeof body.name === 'string' ? body.name.trim() : '';
   const email = typeof body.email === 'string' ? body.email.trim() : '';
   const phone = typeof body.phone === 'string' ? body.phone.trim() : '';
+  const businessName =
+    typeof body.businessName === 'string' ? body.businessName.trim() : '';
+  const taxId = typeof body.taxId === 'string' ? body.taxId.trim() : '';
+
+  // Receipt is issued to the business name when provided (osek patur → קבלה only).
+  const documentName = businessName || name;
 
   if (!name || !email || !phone) {
     return NextResponse.json(
@@ -62,7 +68,7 @@ export async function POST(req: Request) {
       APIKey: apiToken,
     },
     Customer: {
-      Name: name,
+      Name: documentName,
       EmailAddress: email,
       Phone: phone,
       SearchMode: 0,
@@ -75,7 +81,9 @@ export async function POST(req: Request) {
         Currency: 'ILS',
       },
     ],
-    DocumentDescription: 'מוח עסקי - הרשמת מייסד (מקדמה)',
+    DocumentDescription: businessName
+      ? `מוח עסקי - הרשמת מייסד (מקדמה) · איש קשר: ${name}${taxId ? ` · ח.פ/ע.מ: ${taxId}` : ''}`
+      : 'מוח עסקי - הרשמת מייסד (מקדמה)',
     MaximumPayments: 1,
     RedirectURL: `${siteUrl}/thanks?product=business-brain`,
     ExitRedirectURL: `${siteUrl}/products/business-brain?payment=cancel`,
